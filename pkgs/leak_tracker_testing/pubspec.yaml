name: leak_tracker_test
version: 0.0.1
description: Leak tracking code intended for usage in tests.
repository: https://github.com/dart-lang/leak_tracker

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  leak_tracker: ^7.0.4
  test: ^1.16.0

dev_dependencies:
  flutter_lints: ^2.0.0
  layerlens:
