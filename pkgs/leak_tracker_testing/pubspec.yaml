name: leak_tracker_test
version: 0.0.1
description: Leak tracking code intended for testing only.
repository: https://github.com/dart-lang/leak_tracker

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  leak_tracker: ^7.0.0

dev_dependencies:
  flutter:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  layerlens:
  integration_test:
    sdk: flutter
