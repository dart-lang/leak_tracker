include: package:lints/recommended.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true

linter:
  rules:
    - avoid_catching_errors
    - avoid_print
    - comment_references
    - only_throw_errors
