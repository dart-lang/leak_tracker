name: web_tests
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0-140.0.dev

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  dart_flutter_team_lints: ^3.1.0
  flutter_test:
    sdk: flutter
  layerlens: ^1.0.16

flutter:
  uses-material-design: true
