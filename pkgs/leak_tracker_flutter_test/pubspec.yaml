name: leak_tracker_flutter_test
version: 0.0.1
description: Leak tracking tests that depend on Flutter framework.
repository: https://github.com/dart-lang/leak_tracker

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  flutter:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  leak_tracker: ^7.0.4
  layerlens:
  meta: ^1.8.0

