name: leak_tracker_minimal_dart_example
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  leak_tracker: ^1.0.0-dev.1.3

dev_dependencies:
    flutter_lints: ^3.0.0

dependency_overrides:
  leak_tracker:
    path: ..
