name: leak_tracker_root

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  flutter_lints: ^2.0.0

# TODO(polina-c): remove this file as soon as bug is fixed
# https://github.com/Dart-Code/Dart-Code/issues/4574
